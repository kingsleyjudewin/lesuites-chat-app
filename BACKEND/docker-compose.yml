version: "3.9"

services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  encryption-service:
    build: ./encryption-service
    restart: unless-stopped
    env_file: ./encryption-service/.env
    ports:
